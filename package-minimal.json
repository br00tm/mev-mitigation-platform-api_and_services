{
  "name": "mev-backend-minimal",
  "version": "1.0.0",
  "description": "Backend minimalista para MEV Mitigation Platform",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index-minimal.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist node_modules",
    "install:fast": "npm install --no-optional --no-audit --no-fund"
  },
  "dependencies": {
    "fastify": "^4.24.3",
    "ethers": "^6.8.1",
    "dotenv": "^16.3.1",
    "@fastify/cors": "^8.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.2",
    "tsx": "^4.4.0",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
